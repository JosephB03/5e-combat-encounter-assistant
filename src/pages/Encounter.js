import React from 'react';

function Encounter({encounter}) {
    return (
        <section>
            <h1>Encounter Page</h1>
            <p>This is the Encounter page of the application.</p>
        </section>
    );
};

export default Encounter;
